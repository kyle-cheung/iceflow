review_agent = "claude-code"
review_guidelines = """
For Rust files (.rs), delegate the review to the rust-reviewer subagent.
For C++ files (.cpp, .cc, .h, .hpp), delegate to the cpp-reviewer subagent.
For Python files (.py), delegate to the python-reviewer subagent.
For SQL or database-related files, delegate to the database-reviewer subagent.
Always run the security-reviewer subagent on any code touching authentication,
credentials, or external data boundaries.
"""
